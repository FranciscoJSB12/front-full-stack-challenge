import { Layout } from '../../../ui/components';
import { GameSelector } from '../../components';

export const HomePage = () => {
  return (
    <Layout>
      <div>
        <GameSelector />
      </div>
    </Layout>
  );
};
